{
  "name": "tickle",
  "private": true,
  "scripts": {
    "frontend": "yarn frontend dev",
    "frontend_build": "yarn frontend build",
    "frontend_preview": "yarn frontend preview",
    "storybook": "yarn frontend storybook",
    "build": "turbo run build",
    "dev": "turbo run dev --no-cache --parallel --continue",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy"
  },
  "workspaces": [
    "src/*"
  ],
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
